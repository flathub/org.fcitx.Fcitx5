name: xcb-imdkit
buildsystem: cmake-ninja
sources:
  - type: git
    url: https://github.com/fcitx/xcb-imdkit
    tag: 1.0.2
    x-checker-data:
      type: git
      tag-pattern: "^([\\d.]+)$"

cleanup:
  - /include
  - /lib/pkgconfig
  - /lib/cmake
